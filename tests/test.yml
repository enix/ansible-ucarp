---

- hosts: test
  tasks:
    - name: "Install dependencies"
      apt:
        name: "{{ ansible_python_interpreter | basename }}-addr"
        update_cache: yes
    
- hosts: test
  roles:
    - role: '../../.'
      ucarp__interface: lo
      ucarp__vip: 192.168.42.43
      ucarp__password: totototo
      ucarp__vip_upaction: echo "this is a test" > /tmp/test || true
